# Loaded by the Firebase emulator suite only — never by a real deploy.
#
# App Check has no attestation provider locally, so with enforcement on, the
# functions framework rejects every callable with "unauthenticated" before the
# handler even runs. Production keeps enforcement on by default.
ENFORCE_APP_CHECK=false
